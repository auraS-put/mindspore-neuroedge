# ─────────────────────────────────────────────────────────────────
# auraS — Default top-level configuration
# Composed from sub-configs via Hydra:
#   python -m auras.training.trainer data=siena model=cnn_bilstm_attn training=conv_snn
# ─────────────────────────────────────────────────────────────────
defaults:
  - data: siena
  - model: cnn_bilstm_attn
  - training: conv_snn
  - _self_

seed: 42
project_name: auraS
output_dir: experiments/runs
