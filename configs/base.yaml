project:
  name: auraS
  seed: 42
  device_target: Ascend

paths:
  data_root: data
  runs_dir: experiments/runs

metrics:
  primary: recall
  secondary:
    - f1
    - false_alarm_rate
    - latency_ms
