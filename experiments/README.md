# Experiments

Store reproducible experiment artifacts here.

- `notebooks/`: exploratory analysis and visualization
- `runs/`: tracked outputs per execution

Convention:

- one timestamped folder per run
- include `metrics.json`, `config_snapshot/`, and `checkpoints/`
